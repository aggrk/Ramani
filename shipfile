version: "1.0"

stack: nodejs

build:
  script: npm install && npm run build

run:
  command: HOST=0.0.0.0 PORT=8000 node --watch server.js
  port: 8000
# script: npm install && npm run build

# run: HOST=0.0.0.0 PORT=8000 node --watch server.js
